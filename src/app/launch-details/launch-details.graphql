query launchDetails($id: ID!) {
  launch(id: $id) {
    id
    mission_name
    details
    launch_date_utc
    links {
      flickr_images
      mission_patch
    }
    rocket {
      rocket_name
    }
  }
}
